;;; Commentary:

;;

;;; Code:
(setq load-path
      (append '(
                (expand-file-name "~/lib/emacs/lisp")
                )
              load-path))

(require 'japan-util)
(provide 'compile-file)

;;; compile-file.el ends here
